/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Specify custom output directory if needed
    distDir: 'out', // This ensures the static files will be output to the "out" folder
  };
  
  module.exports = nextConfig;
  